# 런치팝 알리미 - 전체 코드 리뷰

리뷰 날짜: 2026-07-02 | 대상: Master v4.2, Launcher v1.0, Setup v1.0, GAS v2.3
방법: 소스 코드 전체 정독 + 실제 운영 스프레드시트 구조 대조 검증

---

## 🎯 요약: 가장 시급한 것 하나만 고른다면

**GAS 백엔드에 인증이 전혀 없습니다.** URL만 알면 (이미 CLAUDE.md에 공개돼 있고, 배포된 exe 안에도 평문으로 박혀있어 누구나 추출 가능) 누구든지:
- 모든 매장의 고객 이름+주소를 전부 열람 가능
- 모든 주문을 "인쇄완료"로 조작해서 실제로 아무것도 인쇄 안 되게 만들 수 있음 (영업 방해)
- 요청을 무제한으로 보내서 시스템 전체를 마비시킬 수 있음

이건 오늘 당장 실제 피해로 이어질 수 있는 유일한 항목입니다. **다른 건 몰라도 이것만은 최우선으로 처리 권장.**

---

## 🔴 CRITICAL — 즉시 조치 필요 (실제 피해로 이어짐)

### 1. GAS 백엔드 인증 없음 → 전체 고객정보 유출 + 서비스 마비 가능
**파일**: `gas_server.js` (전체 `doGet` 함수)

```
공격 시나리오:
1. GET .../exec?action=getStores  → 매장명 전체 목록 획득
2. GET .../exec?action=fetchV2&storeName=X  (매장마다 반복)
   → 그 매장의 모든 고객 이름 + 주소 전체 열람 (로그인 없이!)
3. GET .../exec?action=markDone&rowIndex=N  (반복)
   → 모든 매장 주문을 "인쇄완료"로 조작 → 실제 음식이 안 나가도 시스템엔 완료로 표시
4. action=log를 무한 호출 → GAS 실행 할당량(quota) 소진 → 전체 서비스 다운
```

실제로 시트를 확인해보니 **8개 매장**의 고객명+주소가 지금 이 방식으로 전부 열람 가능한 상태입니다.

**해결책**: 매장별 비밀 토큰을 발급해서 모든 요청에 `storeToken` 파라미터로 검증하거나, 최소한 고정 API 키 하나라도 `Utilities.computeHmacSha256Signature`로 검증 후에만 데이터 반환.

### 2. 업데이트 메커니즘의 신뢰 근거가 스스로를 검증하는 구조
**파일**: `lunchpop_launcher.py:106-148`

다운로드 URL과 SHA256 해시값이 **같은 출처**(스프레드시트)에서 나옵니다. 즉 SHA256 검증은 "다운로드한 파일이 시트에 적힌 값과 일치하는가"만 확인할 뿐, 그 시트 자체가 조작됐을 때는 아무 방어가 안 됩니다.

**실질적 의미**: 만약 스프레드시트(또는 GAS)에 대한 편집 권한이 누군가에게 뚫리면, 전국 모든 매장 PC에 임의의 실행 파일을 원격으로 심을 수 있습니다 (Launcher가 서명 검증 없이 자동 실행하기 때문).

**해결책**: Master.exe/Launcher.exe에 코드 서명(Authenticode) 적용 + 서명 검증. 최소한 다운로드 URL을 `github.com/jaewoo-heo/lunchpop-pos/releases/*`로 고정(다른 도메인 리다이렉트 차단).

### 3. 프린터 테스트 중 실제 주문이 잘못된 프린터로 인쇄되거나 유실될 수 있음
**파일**: `lunchpop_master.py:993-1009`, `379-423`

설정창에서 "테스트 프린트"를 누르면 전역 변수 `PRINTER_SETTING`을 일시적으로 바꿉니다. 이 짧은 순간에 실제 주문이 들어와서 인쇄를 시도하면, **테스트 중이던 (엉뚱한/연결 안 된) 프린터로 실제 고객 주문이 인쇄되거나 실패**할 수 있습니다.

가게 운영 중 점심시간에 테스트 프린트를 누르는 상황은 충분히 현실적입니다.

**해결책**: 테스트 프린트도 `_print_lock`으로 감싸고, 전역 변수 대신 함수 파라미터로 프린터를 지정하도록 리팩터링.

### 4. markDone 서버 전송 실패 시 재시작하면 중복 인쇄 발생
**파일**: `lunchpop_master.py:492-503`

인쇄는 성공했는데 "인쇄완료" 신호를 서버에 보내는 게 실패하면(3번 재시도 후 포기), 앱은 로컬 메모리에만 "완료"로 기록합니다. 근데 CLAUDE.md에 적힌 설계는 "재시작하면 서버값으로 복구된다"인데, 서버엔 애초에 완료 표시가 안 됐으니 **재시작(정전, 업데이트, 크래시 등) 시 그 주문이 다시 대기 목록에 나타나서 두 번째 영수증이 출력**됩니다.

**해결책**: markDone 실패 건을 로컬에 저장해두고 다음 폴링 때 재시도.

### 5. 특수문자가 든 고객명/메뉴명이 있으면 그 주문은 영원히 인쇄 불가
**파일**: `lunchpop_master.py:439-482`

영수증은 한글 인코딩(CP949)으로 변환하는데, 이모지·중국어·일부 특수기호가 포함된 이름이 들어오면 인코딩 자체가 실패합니다. 이건 **재출력을 몇 번 눌러도 똑같이 실패**하는 결정론적 오류라서, 사장님은 "프린터가 고장났나?" 하고 오해하게 됩니다. 게다가 매 폴링(60초)마다 계속 같은 실패를 반복합니다.

**해결책**: 인코딩 실패 시 문제 문자를 `?` 등으로 대체해서 최소한 영수증은 나오게 처리.

### 6. GAS 응답이 비정상일 때 서버 오류 알림이 뜨지 않고 "연결중"으로만 표시됨
**파일**: `lunchpop_master.py:579-585`

GAS가 정상 JSON이 아닌 응답(할당량 초과 등)을 줬을 때, 대시보드는 심각한 "서버 오류" 빨간 알림 대신 가벼운 "연결 중..." 상태만 계속 보여줍니다. 비전문가 사장님 입장에서는 "그냥 좀 느린가보다" 하고 방치하게 되고, **그 사이 주문이 계속 누락**될 수 있습니다.

**해결책**: 응답이 실제로 유효할 때만 실패 카운트를 초기화하도록 순서 수정.

### 7. 신규 매장 설치 시 자동시작 등록이 조용히 실패해도 "설치 완료"라고 표시됨
**파일**: `lunchpop_setup.py:144-165`

바탕화면 바로가기 생성이나 Windows 자동시작 등록이 실패해도 반환값을 확인하지 않고 무조건 "설치 완료!" 메시지를 보여줍니다. 이 앱의 핵심 가치가 "PC 켜면 자동으로 주문 받기"인데, 이게 실패하면 **다음날 아침 사장님이 주문을 하나도 못 받는데 원인도 모름**.

**해결책**: 각 단계 성공 여부 확인 후 실패 시 명확히 알림.

### 8. 다운로드 중단 시 손상된 설치 파일이 남아서 이후 재설치도 막힘
**파일**: `lunchpop_setup.py:105-123`

Launcher.exe 다운로드가 중간에 끊기면 손상된 파일이 그대로 남고, 다음 실행 시 "이미 설치되어 있습니다"로 인식돼서 사용자가 "재설치 안 함"을 누르면 **손상된 파일을 그대로 실행**하려다 실패 — 복구 방법이 안내되지 않음.

**해결책**: 임시 파일에 먼저 받고 검증 후 이동, 실패 시 파일 삭제.

---

## 🟠 HIGH — 빠른 시일 내 조치 권장

| # | 위치 | 문제 | 실질적 영향 |
|---|------|------|------|
| 9 | `gas_server.js:19-28` | `checkUpdate` 응답에 D2(GitHub 토큰용 셀)가 그대로 노출됨. 실제 시트 확인 결과 현재 값은 `"FALSE"`(토큰 아님)라 당장 유출은 없으나, 안내대로 진짜 토큰을 넣는 순간 그대로 공개 유출됨. 또한 이 필드는 코드 어디서도 실제로 쓰이지 않는 죽은 기능 | 향후 토큰 입력 시 즉시 유출 |
| 10 | `gas_server.js:61-86` | `markDone`의 rowIndex에 상한 검증이 없어 거대한 값을 넣으면 시트가 비정상적으로 커질 수 있음 | 시트 손상 위험 |
| 11 | `gas_server.js:112,143` | 매장명 매칭이 완전일치가 아닌 부분일치(`indexOf`) — "김밥"이 "옆집김밥"에도 매칭될 수 있음 | 매장 간 주문 혼선 가능 |
| 12 | `lunchpop_launcher.py:146-148` | Master.exe가 실행 중일 때 업데이트 교체가 조용히 실패 (사용자에게 아무 알림 없음) | 업데이트가 영원히 안 됨, 아무도 모름 |
| 13 | `lunchpop_launcher.py` (전체) | Launcher 중복 실행 방지 장치 없음 — 두 개가 동시에 뜨면 임시파일이 꼬여서 잘못된 파일이 적용될 위험 | 드물지만 심각한 손상 가능 |
| 14 | `lunchpop_setup.py:104-123` | Launcher.exe 다운로드에는 SHA256 검증이 아예 없음 (Master 업데이트와 다른 기준) | 최초 설치 시점 무결성 공백 |
| 15 | `lunchpop_master.py:523-537` | 프린터 상태 확인 시 예외 발생하면 핸들이 안 닫혀서 누적됨 (24시간 구동 환경) | 장기 운영 중 스풀러 문제 가능 |
| 16 | `lunchpop_master.py:458-480` | 주문 텍스트에 제어문자 필터링 없이 그대로 프린터 명령에 삽입 — 이론상 프린터 명령 주입 가능 | 낮은 확률이나 존재하는 위험 |
| 17 | `lunchpop_master.py` (폴링 루프) | 여러 주문이 한번에 몰리면 순차 처리로 인해 다음 폴링이 지연됨 | 러시아워에 신규 주문 감지 지연 |
| 18 | `gas_server.js` (전체) | try/catch가 없어서 시트 접근 오류 시 JSON 대신 HTML 에러 페이지 반환 → 클라이언트가 파싱 실패 | 드물지만 폴링 스레드에 영향 |
| 19 | `gas_server.js:167-177` | 날짜 리셋 트리거가 P2 셀만 정확히 수정했을 때만 작동 — 행 전체를 붙여넣기 하면 발동 안 함 | 날짜 변경 시 이전 완료 표시가 남아있을 수 있음 |

---

## 🟡 MEDIUM — 여유 있을 때 개선

- **날짜 형식 불일치 잠재 위험**: 실제 시트 확인 결과 현재는 항상 0패딩(`2026.07.02`)이라 문제없지만, 운영자가 수동으로 P2를 다르게 입력하면(`2026/7/2`) 그날 전체 주문이 하나도 안 보이는 사태 가능 (`gas_server.js` 날짜 정규화 로직)
- P2 셀이 비어있으면 전체 과거 주문을 다 반환하는 "안전장치"가 있는데, Master 쪽에 이를 대비한 방어 로직이 없어 대량 재인쇄 위험 (`lunchpop_master.py`)
- 백업 파일(`LunchPop_Master.backup.exe`)은 만들어지지만 실제로 복구에 쓰이는 코드가 없음 — 죽은 안전장치
- 관리자 권한으로 설치 시, 실제 사용자 계정이 아닌 관리자 계정에 바로가기/자동시작이 등록될 위험 (공용 PC 환경)
- 멀티 모니터 환경에서 대시보드 위치가 저장 안 되고 매번 기본 위치로 리셋됨
- 고해상도(DPI) 모니터에서 UI가 흐릿하게 나올 수 있음 (DPI 인식 선언 없음)
- "오전 12시(자정)" 배달시간 처리 오류 — 정오로 잘못 계산될 수 있음
- CI 빌드 시 패키지 버전이 고정되어 있지 않아, 특정 시점에 라이브러리가 업데이트되면 예고 없이 빌드 결과물이 달라질 수 있음

---

## 🟢 LOW — 참고용

- 첫 설정 시 "직접입력" 옵션을 선택하고 지우지 않으면 매장명이 문자 그대로 "직접입력"으로 저장되는 UX 함정
- 로그 파일 회전 실패 시 그 이후 로그가 전부 유실될 수 있음
- 스레드 간 공유되는 일부 set 자료구조에 명시적 락이 없음 (현재는 안전하나 향후 리팩터링 시 위험)
- `getStores`가 빈 시트일 때 `["매장목록 없음"]`이라는 문자열을 배열에 넣어 반환 (API 설계상 사소한 결함)

---

## ✅ 잘 되어있는 부분 (참고)

- 로그에 고객명을 `김*현(3537)`처럼 부분 마스킹해서 기록하는 습관은 좋은 개인정보 보호 사례
- CI의 SHA256 계산 방식(PowerShell ↔ Python)이 알고리즘/인코딩 모두 정확히 일치 — 이 부분은 문제 없음
- GitHub Actions 권한(`contents: write`)이 최소 권한 원칙을 따르고 있음

---

## 📋 우선순위 액션 플랜 (제안)

| 순서 | 항목 | 예상 작업량 |
|------|------|------|
| 1 | GAS에 매장별 토큰 인증 추가 | 중 (GAS + Master 양쪽 수정) |
| 2 | markDone rowIndex 상한 검증 추가 | 소 |
| 3 | 테스트 프린트 락 처리 | 소 |
| 4 | 인코딩 실패 시 대체문자 처리 | 소 |
| 5 | 설치/업데이트 실패 시 사용자 알림 추가 | 중 |
| 6 | markDone 실패 시 로컬 재시도 큐 | 중 |

원하시면 이 중 몇 개를 골라서 바로 수정 작업 들어갈 수 있습니다.
